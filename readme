обработка входящих писем гмайл на почту

1)аутифицируемся в почту (из файла token.json берется токен)
2)берем k последних почт
3)передаем данные из почты в mistral для обработки
4)собираем все и сохраняем в файл output.txt